/* 
 * Author: Petr Kozler
 */

#ifndef CLIENT_SOCKET_H
#define CLIENT_SOCKET_H
/*
#include <stdbool.h>
#include <pthread.h>

typedef struct {
    pthread_t thread; // vlákno pro čtení požadavků klienta
    pthread_mutex_t recv_lock; // zámek klienta pro příjem
    pthread_mutex_t send_lock; // zámek klienta pro zápis
    int sock; // deskriptor socketu klienta
    bool connected; // příznak připojení klienta
} client_socket_t;
*/
#endif /* CLIENT_SOCKET_H */

